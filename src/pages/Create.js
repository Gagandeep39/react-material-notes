import { Typography } from '@material-ui/core';
import React from 'react';

const Create = () => {
  return (
    <div>
      <Typography
        variant="h6"
        color="textSecondary"
        component="h2"
        gutterBottom
      >
        Create a new Note
      </Typography>
    </div>
  );
};

export default Create;
